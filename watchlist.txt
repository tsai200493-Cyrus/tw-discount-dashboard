# 追蹤清單：一行一檔台股代號，# 後面可寫名稱（會顯示在網站上）。
# 想加股票就加一行、想刪就刪一行 —— 手機上直接在 GitHub 編輯這個檔、Commit，網站幾分鐘後自動更新。
#
# 進階：改「本益比位階」的比較起算日（預設近 5 年滾動）
#   6669  # 緯穎 @since=2023-01 AI伺服器ODM轉型，之前是低毛利代工
#   理由必填，沒寫就不生效；樣本不足 200 個交易日會自動退回近 5 年。卡片上會標示用的是哪個區間。
#   設了之後網站上會多一組「比較區間：近5年 / 客製」切換鈕，兩套數字都算得出來、隨時對照。
#   ⚠ 少用。縮短區間＝刪掉低估值年代，實測會讓追蹤清單幾乎每一檔都變便宜，
#     只該用在「這家公司已經不是同一家公司了」的情況，不是用來讓股票看起來比較便宜。
2330  # 台積電 @since=2024-07 先進製程定價權，營益率自2024Q3階梯推升 42.6%→59.3%
6669  # 緯穎
2382  # 廣達 @since=2023-01 成長主軸從NB轉AI伺服器，營益率1.8%→4.4%
3037  # 欣興
3481  # 群創
2454  # 聯發科
2408  # 南亞科
2308  # 台達電
1815  # 富喬
5475  # 德宏
